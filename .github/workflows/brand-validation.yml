name: Brand

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Verify image assets exist (placeholder)
        run: |
          test -d custom_components/bromic_smart_heat_link || exit 0
          echo "OK"
